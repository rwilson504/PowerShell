# Dataverse / SiteMap

Sitemap XML tooling.

## Scripts

| Script | Purpose | WithAuth pair? |
|---|---|---|
| [ConvertSitemapToCSV.ps1](ConvertSitemapToCSV.ps1) | Parses a model-driven app sitemap XML file and converts it to a flat CSV (Area / Group / SubArea hierarchy with titles, entity bindings, URLs, and icons) for review or audit. | n/a — operates on local XML, no API auth needed |
